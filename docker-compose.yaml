services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: business_card
      POSTGRES_PASSWORD: business_card
      POSTGRES_DB: business_card
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
